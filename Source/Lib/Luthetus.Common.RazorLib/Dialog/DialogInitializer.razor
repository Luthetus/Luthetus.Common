@inherits Fluxor.Blazor.Web.Components.FluxorComponent

@{
    var dialogRecords = DialogRecordsCollectionWrap.Value.DialogRecords;

    foreach (var dialogRecord in dialogRecords)
    {
        <DialogDisplay @key="dialogRecord.DialogKey" 
                       DialogRecord="dialogRecord" />
    }
}